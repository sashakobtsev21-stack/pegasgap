# Список прокси, по одному в строке. Формат ровно тот, в котором их обычно выдают,
# чтобы можно было вставить как есть:
#
#     host:port:логин:пароль
#     host:port                 — если авторизации нет
#
# Пустые строки и строки с # игнорируются.
#
# СКОПИРУЙТЕ ЭТОТ ФАЙЛ В proxies.txt и вставьте туда свой список.
# proxies.txt в .gitignore — в репозиторий он не попадёт. Сам этот пример держите
# без реальных адресов.
#
# 203.0.113.10:8000:user:password
# 203.0.113.11:8000:user:password
